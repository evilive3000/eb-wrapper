export enum VerdictChangedSubs {
  MediaSub = 'verdict-changed-media-subs',
}

export enum MediaUpdatedSubs {
  SearchSub = 'media-updated-search-subs',
}

export enum ErrorCaughtSubs {
  sub = 'error-caught-subs',
}
